#!/usr/bin/node
/**
 * Represents a rectangle.
 */
class Rectangle {
  constructor(w, h) {
    // Initialize width and height attributes
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
